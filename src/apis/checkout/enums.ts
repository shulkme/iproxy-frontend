export enum PAYMENT_METHOD_ENUM {
  CREDIT = 'credit',
  CRYPTO = 'crypto',
  LOCAL = 'local',
}

export enum ORDER_STATUS_ENUM {
  SUCCESS = 'success',
  FAILED = 'fail',
  PENDING = 'pending',
  CANCELLED = 'cancelled',
  EXPIRED = 'expired',
  REFUNDED = 'refunded',
}
