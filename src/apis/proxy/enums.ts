export enum PROXY_STATUS_ENUM {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  EXPIRED = 'expired',
  DELETED = 'deleted',
}

export enum PROXY_STATUS_ENUM {
  SUCCESS = 'success',
  FAILED = 'fail',
  PENDING = 'pending',
  CANCELLED = 'cancelled',
  EXPIRED = 'expired',
  REFUNDED = 'refunded',
}
